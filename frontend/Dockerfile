FROM node:24-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci  

COPY . .
RUN npm run build

# Only copy what exists
RUN cp -r .next/static .next/standalone/.next/

ENV PORT=3000
EXPOSE 3000

CMD ["node", ".next/standalone/server.js"]